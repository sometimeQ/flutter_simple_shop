name: demo1
description: A new Flutter application.
version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  url_launcher: ^5.4.2
  flutter_webview_plugin: ^0.3.0+2
  date_format: ^1.0.4
  shared_preferences: ^0.5.6+3
  cupertino_icons: ^0.1.2
  dio: ^3.0.9
  flutter_spinkit: ^4.1.2+1
  fluttertoast: ^4.0.0
  flutter_screenutil: ^1.1.0 # 屏幕适配
  flutter_swiper : ^1.1.6 # 轮播图
  provider: ^4.0.5 # 状态管理
  gbk2utf8: ^1.0.0
  fluro: ^1.6.3 # 路由
  flutter_easyrefresh: ^2.1.1   # 上拉加载更多
  flutter_custom_dialog: ^1.0.19  # 弹窗
  pull_to_refresh: ^1.5.7 # 上拉加载
  fbutton: ^1.0.4
  fradio: ^1.0.1
  fsuper: ^0.1.5
  flustars: ^0.3.2
  common_utils: ^1.2.1
  extended_image: '0.7.2'
  extended_image_library: '0.2.1'
  waterfall_flow: ^0.1.5  # 国产瀑布流
  loading_more_list: ^1.0.5
  extended_list: ^0.1.5
  pull_to_refresh_notification: ^0.3.4
  shimmer: ^1.1.1 #骨架屏
  flutter_svg: ^0.17.4
  extended_text_field: ^0.5.0
  extended_text: ^0.7.1
  flutter_emoji: ^2.2.1
  html: ^0.14.0+3
  image_picker: ^0.6.6+5
  extended_tabs: ^0.2.3
  luhenchang_plugin: ^0.1.2
  flutter_alibc: ^0.0.15 # 淘宝登录


dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true

  assets:
     - assets/images/
     - assets/nav/
     - assets/icons/
     - assets/svg/
     - assets/json/

